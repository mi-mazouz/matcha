# source: http://localhost:8080/graphql
# timestamp: Thu Sep 06 2018 11:31:23 GMT-0700 (PDT)

"""
Birthdate format.
"""
scalar Date

type Query {
  getPictures(userId: ID): Pictures
  getUser(id: ID): User
}

type Picture {
  path: String
}

type Pictures {
  profilePicture: Picture
  pictures: [Picture]
}

"""
The `Upload` scalar type represents a file upload promise that resolves an
object containing `stream`, `filename`, `mimetype` and `encoding`.
"""
scalar Upload

type User {
  email: String
  firstName: String
  lastName: String
  gender: String
  sexualOrientation: String
  username: String
  birthDate: Date
}
