scalar Birthdate

type Query {
  getUser(userId: ID): User
}

type User {
  id: Int
  email: String
  firstName: String
  lastName: String
  gender: String
  sexualOrientation: String
  username: String
  birthDate: Birthdate
}
